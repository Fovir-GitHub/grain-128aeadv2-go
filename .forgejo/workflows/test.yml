name: go-test
on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  test:
    runs-on: golang
    steps:
      - uses: actions/checkout@v6
      - name: Run Tests
        run: go test ./... -v
